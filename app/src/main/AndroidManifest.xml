<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sgcdeveloper.moneymanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/money_manager_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/money_manager_icon"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/Theme.MoneyManager">
        <activity
            android:screenOrientation="portrait"
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MoneyManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>